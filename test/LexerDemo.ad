use os
from vegetables use oranges // doesn't exist :)

struct Fruits {
    name: String,
    type: String,
    id:   UInt64
}

enum Fruits {
    Apple,
    Orange,
    Jackfruit,
    Banana
}

func main() {
    # Execute this piece of code
    show(f"os::pwd = {os::pwd}")
    show("That's it!")
}